version: '3.8'

services:
  soil-moisture-app:
    build: .
    container_name: soil_moisture_container
    restart: always
    ports:
      - "3000:3000"
    environment:
      - PORT=3000
      - NODE_ENV=production
    volumes:
      - ./data:/app/data
